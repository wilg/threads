= form_for @message_thread do |f|
  - if @message_thread.errors.any?
    #error_explanation
      %h2= "#{pluralize(@message_thread.errors.count, "error")} prohibited this message_thread from being saved:"
      %ul
        - @message_thread.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
